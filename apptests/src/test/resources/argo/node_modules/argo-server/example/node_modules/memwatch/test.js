var memwatch = require('./');

try {
  var hd = new memwatch.HeapDiff();
  var arr = [1,2,3,4,5,"hi"];
  hd.end();
  hd.end();
} catch(e) {
  console.log(e);
}

for (var i = 0; i < 1e3; i++) {
  if (!(i % 1e2)) console.log(i, ":", process.memoryUsage());
  var hd = new memwatch.HeapDiff();
  var arr = [1,2,3,4,5,"hi"];
  hd.end();
}
